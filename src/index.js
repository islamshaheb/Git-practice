/** @format */

import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Bgcolor from './backgrounds';

ReactDOM.render(
  <React.StrictMode>
    <Bgcolor />
  </React.StrictMode>,
  document.getElementById('root')
);
